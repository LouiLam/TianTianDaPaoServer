<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="client.login.DaPaoLoginDao">

	<resultMap id="ormUser" type="User">
	</resultMap>
	<select id="selectUserByUpuid" parameterType="Map" resultType="Map">
		select userinfo.* from userinfo where id = #{id} and password =
		#{password}
	</select>

	<select id="insertUserIntoUserInfo" parameterType="Map">
		insert into
		userinfo (upuid, upid, uname, uface, ugender, urtime)
		values (#{upuid},
		#{upid}, #{uname}, #{uface}, #{ugender}, #{urtime})
	</select>

	<select id="insertUserIntoGameTable" parameterType="Map">
		insert into
		gdmj (ultime, uid, utoken) (select urtime, max(uid), #{utoken}
		from
		userinfo)
	</select>

	<select id="updateUserInfoUltime" parameterType="Map">
		update userinfo
		set utoken=#{utoken}, ultime=#{ultime}
		,uconsecutive=#{uconsecutive}
		where uid = #{uid}
	</select>

	<select id="updateUserinfoSetUname" parameterType="Map">
		update
		userinfo set uname=#{uname}
	</select>
	<select id="login_reward" statementType="CALLABLE"
		parameterType="Map">
		call login_reward(#{uid, mode=IN, jdbcType=INTEGER})
	</select>






	<select id="selectUstatusFromGDMJ" parameterType="Map">
		select count(*)
		from userinfo
		where upuid = #{upuid} and upid = #{upid}
	</select>

	<select id="loginUpdateUltimeByUserInfo" parameterType="Map">
		update
		usergame set ultime=#{ultime} where uid = #{uid}
	</select>

	<select id="selectJJCUserByUtoken" parameterType="Map"
		resultType="Map">
		select userjjc.* ,userinfo.id,userinfo.is_get_this_day from userinfo,userjjc where
		userinfo.uid =
		userjjc.uid and userinfo.utoken= #{utoken}
	</select>

</mapper>